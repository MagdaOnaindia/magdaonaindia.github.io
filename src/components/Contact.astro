<section id="contact" class="section" aria-labelledby="contact-title">
  <div class="container contact-inner">
    <p class="section-kicker">say hi</p>
    <h2 id="contact-title">Contact</h2>
    <p class="pitch">
      Building something with AI agents? Hiring product-minded engineers? Let's talk.
    </p>
    <div class="email-row">
      <a class="email" href="mailto:magdaonvi2@gmail.com">magdaonvi2@gmail.com</a>
      <button id="copy-email" type="button" class="btn btn-ghost">Copy</button>
    </div>
    <ul class="links">
      <li><a href="https://github.com/MagdaOnaindia">GitHub ↗</a></li>
      <li><a href="https://www.linkedin.com/in/magdalenaonaindia">LinkedIn ↗</a></li>
    </ul>
  </div>
</section>

<style>
  .contact-inner {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .pitch {
    font-size: 1.125rem;
    color: var(--ink-muted);
  }

  .email-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: var(--s-3);
    margin-block: var(--s-4) var(--s-6);
  }

  .email {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 3.5vw, 1.75rem);
    font-weight: 600;
  }

  .links {
    display: flex;
    gap: var(--s-6);
    list-style: none;
    margin: 0;
    padding: 0;
  }
</style>

<script>
  document.getElementById('copy-email')?.addEventListener('click', async () => {
    await navigator.clipboard.writeText('magdaonvi2@gmail.com');
    window.dispatchEvent(
      new CustomEvent('app:toast', { detail: 'Copied! / Kopiatuta!' }),
    );
  });
</script>
